# Supabase Configuration (when ready to integrate)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Database Configuration
DATABASE_URL=postgresql://neondb_owner:npg_3ZfH9TYRIXgP@ep-wild-dawn-ael0ur5n-pooler.c-2.us-east-2.aws.neon.tech/ngk?sslmode=require&channel_binding=require

NETLIFY_DATABASE_URL=postgresql://neondb_owner:npg_3ZfH9TYRIXgP@ep-wild-dawn-ael0ur5n-pooler.c-2.us-east-2.aws.neon.tech/ngk?sslmode=require&channel_binding=require

# Storage Configuration
STORAGE_BUCKET=ngk-hr-storage

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# Authentication
# Generate a secure JWT secret for session management
AUTH_SECRET="I9876TFGHJKLPOIUYtfghjo0-987tyfhgJKPO0987yughjKLPO098tyghJKPO0987ghklpo89YUG"

# Email Service (optional)
SENDGRID_API_KEY=your_sendgrid_key_here

# Feature Flags
ENABLE_DOCUMENT_PREVIEW=true
ENABLE_REPORTS_EXPORT=true



===============================
when i deploy in netlify i get a neion databse id like to use to set it up when i deploy the project

Next steps
To keep your database instance active after the initial 7-day period, connect to Neon and claim your database.

Visit our documentation for more details:
Learn more about Netlify DB

To get data from your Neon database directly using the `@netlify/neon` package, you can use this query:

import { neon } from '@netlify/neon';
const sql = neon(); // automatically uses env NETLIFY_DATABASE_URL
const [post] = await sql`SELECT * FROM posts WHERE id = ${postId}`;

Environment variables
NETLIFY_DATABASE_URL
NETLIFY_DATABASE_URL_UNPOOLED


Neon
Netlify DB is powered by Neon and is optimized for AI workflows so that you can provision a Postgres database with a single command:

npx netlify db init

That’s it. Your database is provisioned, connected, and ready to use. Netlify DB, powered by Neon, is built for AI-native development. With a single command—or prompt—you get:

An instantly provisioned PostgreSQL database
Drizzle ORM and Studio configured automatically
Environment variables securely connected to your Netlify project

Database
steep-scene-58805980
Owned by

Environment variables
NETLIFY_DATABASE_URL
NETLIFY_DATABASE_URL_UNPOOLED
